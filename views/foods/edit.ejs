<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a Food</title>
</head>
<body>
    <!-- update the header -->
    <h1>Edit <%= food.name %></h1>
    <form action="/foods/<%=food._id%>?_method=PUT" method="POST">
      <label for="name">Name:</label>
      <input type="text" name="name" value="<%= food.name %>" />
      <label for="isReadyToEat">Ready to Eat?</label>
      <input type="checkbox" name="isReadyToEat" <% if (food.isReadyToEat) { %>checked<% } %> >
      <!-- update the button text -->
      <button type="submit">Update Food</button>
    </form>
    <a href="/foods">Back to Foods</a>
  </body>