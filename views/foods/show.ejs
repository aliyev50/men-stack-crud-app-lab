<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= food.name %></title>
  </head>
  <body>
    <h1><%= food.name %></h1>
    <% if (food.isReadyToEat) { %>
        <p>This food is ready to eat!</p>
      <% } else { %>
        <p>This food is not ready to eat!</p>
      <% } %>
      <a href="/foods/">Back to Foods</a>

  </body>
</html>
